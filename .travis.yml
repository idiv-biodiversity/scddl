sudo: false

os: linux
dist: trusty

language: generic

addons:
  apt:
    packages:
      - lftp
      - tree

before_script:
  - mkdir /tmp/db

script:
  - bash ncbidl.sh -v -p 2 /tmp/db blast/db/vector
  - tree /tmp/db
